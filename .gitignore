# macOS
.DS_Store

# Editor
.vscode/
*.swp

# Sensitive — never commit
.env
*.key
service-account*.json

# Seed scripts and proxy — run locally, do not commit
seed-firestore.js
seed-config.js
proxy.js
weekly_lm_dashboard.html

# Node dependencies for seed script
node_modules/
