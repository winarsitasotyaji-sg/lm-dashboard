# macOS
.DS_Store

# Editor
.vscode/
*.swp

# Sensitive — never commit
.env
*.key
service-account*.json

# Seed scripts — run locally, do not commit
seed-firestore.js
seed-config.js
weekly_lm_dashboard.html
# proxy.js is committed — it has no credentials, safe to be public

# Node dependencies for seed script
node_modules/
